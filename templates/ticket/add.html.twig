{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Ticket</h1>
    {{ form_start(form) }}
    {{ form_row(form.subject) }}
    {{ form_row(form.description) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}